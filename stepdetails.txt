# vendor lock-in:
    -- we should write our code in such a way that we can move our code from one app to another if needed without much changes
    -- Never create a vendor lock-in

Appwrite database, file upload and custom queries

# Database Service with Appwrite:
    -- To avoid vendor lock-in we create Services that talk to backend. 
        -- creating a service named as "appwrite"
            -- parallel to auth.js we will create a file for db as well   https://appwrite.io/docs/references/cloud/client-web/databases 
                -- queries can be run on indexes.