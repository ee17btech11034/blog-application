# vendor lock-in:
    -- we should write our code in such a way that we can move our code from one app to another if needed without much changes
    -- Never create a vendor lock-in



# components:
    -- Real time Editor (RTE):
        -- Using COntroller for this instead of useRef hook.
    
    -- We will take the values from Editor and handle it in "post-form" component.

    