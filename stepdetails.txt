# vendor lock-in:
    -- we should write our code in such a way that we can move our code from one app to another if needed without much changes
    -- Never create a vendor lock-in



# components:
    -- login/logout page:
        -- we will build these components using "react Hook form"   https://react-hook-form.com/ 
        -- LogIn component is done similar way we can create a SignUp. 
    
    -- Authlayout.jsx : 
        -- till now we did not write any protection mechanism. 
        -- we create a container which is protected and decides if values needs to be shown or not.