# vendor lock-in:
    -- we should write our code in such a way that we can move our code from one app to another if needed without much changes
    -- Never create a vendor lock-in



# pages:
    -- create a folder "pages"
    -- create specific .jsx files for pages. Like Login, logout, AddPost, EditPost, etc

# router:
    -- We will wrap routes in AUthLayout


# CORS: 
    -- Browser does not serve scross.
    -- Suppose server is on another port in another computer and frontend is on another compoter another port. 
    -- Browser blocks these request as browser can not trust. 

    -- CORS is handled in backend 
    
    -- In appwrite:
        -- go to project > Integration > Add platform {Web}
            -- user name "localhost" or "*vercel"


# deploy fronend.

# Assignments:
    -- 